# TODO

Two rules to move. Neither adds content; both fix where a rule sits.

- [ ] **Attach the deliverable exemption to the opening imperative.** It currently sits 600 words below the line it modifies, so drafts and explainers get reshaped before the reader reaches it. Line one should read: resend the last reply in the shape below, unless that reply was the deliverable itself.
- [ ] **Lift the plain-language rule out of "Offering a choice".** "Name it for what it does, not what it is called" applies to every reply, not only to options, and only fires under that heading today.

Rerun `c-override-real-writing` and `c-jargon-options` after both.
